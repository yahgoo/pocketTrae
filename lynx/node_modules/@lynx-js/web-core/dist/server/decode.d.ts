export interface DecodedTemplate {
    config: Record<string, any>;
    styleInfo?: Uint8Array;
    lepusCode: Record<string, Uint8Array>;
    customSections?: Record<string, any>;
}
export declare function decodeTemplate(buffer: Uint8Array, transformVW: boolean, transformVH: boolean, transformREM: boolean): DecodedTemplate;
