export default async function encode({ encodeBinary = undefined, encodeOptions, tasmPkg = '@lynx-js/tasm', }) {
    const { getEncodeMode } = (await import(tasmPkg));
    // Napi will be used if supported
    const encode = getEncodeMode(encodeBinary);
    return encode(encodeOptions);
}
//# sourceMappingURL=encode.js.map