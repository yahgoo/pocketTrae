/**
 * Global state shared across modules to avoid circular dependencies
 */
/**
 * List of background snapshot instances to remove during commit phase
 */
export declare let globalBackgroundSnapshotInstancesToRemove: number[];
export declare function setGlobalBackgroundSnapshotInstancesToRemove(ids: number[]): void;
